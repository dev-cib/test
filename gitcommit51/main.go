package main

import "fmt"

func main() {
	fmt.Println("Hello, we're working on versions")
}

// HAD "fatal: Not a git repository"
// to fix (git init) - i had to re-initialize git in directory

// to delete a tag (git tag -d (tag name))
