package main

import "fmt"

func main() {
	fmt.Println("hello gophers")
}

// look at contents in folder (ls -la)
// build your program (go build main.go) (go build .) (go build ./...)
// run your executable = ./<name>
